<Window x:Class="PhoneBookWpf.EditContactWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Редактировать контакт" Height="200" Width="400"
        WindowStartupLocation="CenterOwner"
        ResizeMode="NoResize">

    <Grid Margin="15">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="80"/>
            <ColumnDefinition Width="*"/>
        </Grid.ColumnDefinitions>

        <Label Grid.Row="0" Grid.Column="0" Content="Имя:" VerticalAlignment="Center"/>
        <TextBox Grid.Row="0" Grid.Column="1" 
                 x:Name="NameBox"
                 Margin="0,5" Padding="3"/>

        <Label Grid.Row="1" Grid.Column="0" Content="Телефон:" VerticalAlignment="Center"/>
        <TextBox Grid.Row="1" Grid.Column="1" 
                 x:Name="PhoneBox"
                 Margin="0,5" Padding="3"/>

        <StackPanel Grid.Row="2" Grid.Column="1" 
                    Orientation="Horizontal" HorizontalAlignment="Right"
                    Margin="0,10,0,0">
            <Button Content="Сохранить" 
                    x:Name="SaveButton"
                    Click="SaveButton_Click"
                    Padding="10,5" Margin="0,0,10,0"/>
            <Button Content="Отмена"
                    x:Name="CancelButton"
                    Click="CancelButton_Click"
                    Padding="10,5"/>
        </StackPanel>
    </Grid>
</Window>